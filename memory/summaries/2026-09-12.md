# 2026-09-12 每日总结

## 完成的任务

1. **修复自动化任务全线失效（00:12）** — `git-backup` 连续失败 151 次被自动禁用，根因是 `agents.defaults.modelPolicy.allow` 只允许 `deepseek/deepseek-flash`，而多个任务硬编码了 `ark/glm-5.1` 模型覆盖，预检直接拒绝。已清除 `git-backup`、`daily-summary`、`nightly-security-audit`、`AI-daily-news` 四个任务的模型覆盖/fallback，并对齐 delivery 配置；`git-backup` 手动触发验证成功（提交 `96d5f45` 已推送，工作区干净）。 [[project: github.com/zjwson123/openclaw-brain-backup-v2]]
2. **定位夜间安全巡检 2 个 warning（03:16）** — 配置基线哈希失败的原因查明：9/11 21:10 的 OpenClaw 升级到 `2026.9.4` 重写了 `openclaw.json`（模型供应商收敛、新增 modelPolicy 白名单），这正是当晚自动化失效的同一个根因。已重新校准基线（旧基线备份保留），`shasum -c` 校验通过。11304 个文件变更经核查为升级安装产物 + 备份仓 git 对象，判定噪音。
3. **日常自动化运行** — 修复后 `daily-summary`、`nightly-security-audit`、`AI-daily-news` 恢复调度。

## 得失

- **得**：把"151 次失败"的表象追到"升级改写了全局模型白名单"这一个根因上，一次性修好 4 个任务；安全巡检的配置漂移告警真实有效，不是误报。
- **失**：升级（2026.9.4）与自动化失效之间隔了一段时间才被发现，说明失败告警的收敛不够快 —— 151 次失败说明告警阈值/可达性本身也有问题（告警发不出去才堆到 151 次）。另外配置基线在升级后需要人工重校准，属于可自动化的环节。

## 建议

1. **失败告警自检**：连续失败应在 3 次内触达用户；建议给自动化加一条"告警通道健康检查"，避免告警静默堆积。
2. **待用户决策**：是保持"任务对齐 allowlist 内模型"，还是把 `ark/glm-5.1` 加回 `modelPolicy.allow`（涉及全局模型策略，未擅自放宽）。
3. **升级预案**：OpenClaw 升级前先备份 `openclaw.json`，升级后立即跑一次配置基线校准 + 自动化冒烟测试。
4. **健康提醒**：今天的工作集中在凌晨 00:12 和 03:16，仍是熬夜节奏，建议把巡检/修复类工作挪到白天。
