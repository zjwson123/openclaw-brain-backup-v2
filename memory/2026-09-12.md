# 2026-09-12 日志

## 🔧 自动化任务修复（00:12）

**触发**：cron 提醒 — automation `git-backup` (72009eb3) 连续失败 151 次后被自动禁用。

**根因**：`agents.defaults.modelPolicy.allow` 被限制为仅 `["deepseek/deepseek-flash"]`，但多个自动化任务都硬编码了模型覆盖 `ark/glm-5.1`（+ fallbacks），预检直接拒绝 → 每次运行 100ms 内失败。

**处理**（黄线操作，已记录）：
- `git-backup`：`--clear-model --clear-fallbacks`，delivery 改为 `none`（静默，仅失败告警），重新 enable。手动触发验证 → `ok succeeded`，提交 `96d5f45 chore: backup workspace 2026-09-12 00:12 CST`，已推送，工作区干净。 <!-- project: github.com/zjwson123/openclaw-brain-backup-v2 -->
- `daily-summary` (ca7862be)：清除模型覆盖（delivery 原本已显式配置 feishu → ou_e998a002b330bcd01abb649a36f168ae）。
- `nightly-security-audit` (c37e670f)：清除模型覆盖 + delivery 设为 announce → feishu/ou_e998a002b330bcd01abb649a36f168ae。
- `AI-daily-news` (d4d5d39c)：清除模型覆盖 + delivery 设为 announce → feishu/ou_e998a002b330bcd01abb649a36f168ae。

**待用户确认**：是保持当前"对齐到 allowlist 内模型"，还是把 `ark/glm-5.1` 加回 `modelPolicy.allow`（涉及全局模型策略，未擅自放宽）。

**备注**：`heartbeat:main` 最近一次为 `no-route`（心跳跳过），未改动。

## 🛡️ 夜间安全巡检排查（03:16）

巡检报出 2 个 warning，已定位根因：

**1) 配置基线哈希失败** — 根因已确认：2026-09-11 21:10–21:15 有一次 OpenClaw 升级到 `2026.9.4`，重写了 `openclaw.json`：
- `meta.lastTouchedVersion` 2026.7.1-2 → 2026.9.4，新增 `migrations.modelPolicyAllowlist`
- models.providers 从多供应商（ark: kimi-k2.6 / glm-5.1 等）收敛为仅 `deepseek/deepseek-flash`，并新增 `agents.defaults.modelPolicy.allow`
- 移除 `commands.ownerDisplay`，新增 `agents.entries.main`
- 这**正是当晚自动化全部失效的原因**（旧任务仍覆盖 ark/glm-5.1 → 被白名单拒绝）
- 处置：已重新校准基线（旧基线备份为 `.config-baseline.sha256.bak-20260912`），`shasum -c` 校验 OK

**2) 11304 个文件变更** — 分布：`npm/projects` 9648、`.git/objects` 4809、`cache/control-ui-assets` 1306，其余为 agents/workspace memory 等。均为升级安装产物 + 备份仓 git 对象，无异常可执行文件，判定为噪音。 <!-- project: github.com/zjwson123/openclaw-brain-backup-v2 -->
